#include <iostream>
#include <cmath>
#include <string>


using namespace std;

/** função de inicialização do programa **/
int main() {

    string s;
    int num, aux, op, resto, resultado;

    cout << " Escolha a operação que quer fazer: " << endl;
    cout << " 1 = decimal para binario" << endl;
    cout << " 2 = binario para decimal" << endl;
    cout << " 0 = Fechar APP" << endl;
    cin >> op;

    if (op == '1') {
        cout << endl << "Qual o num a converter? ";
        cout << endl << "Num: ";
        cin >> num;
       // do {}
    }



        return 0;
}
